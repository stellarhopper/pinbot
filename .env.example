# Bot token from https://discord.com/developers/applications
DISCORD_TOKEN=

# Optional. Set to a test server's ID and slash commands sync to that server
# instantly instead of globally (global sync can take up to an hour to appear).
# Leave unset in production so the bot works in every server it is added to.
DEV_GUILD_ID=

# Optional. Where the SQLite database lives. Default: data/pinball.db
DB_PATH=

# Optional, phase 2 only. Enables the photo/score cross-check, which also has
# to be switched on per server with /config vision on.
ANTHROPIC_API_KEY=
